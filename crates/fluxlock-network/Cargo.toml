[package]
name = "fluxlock-network"
version = "0.1.0"
edition = "2021"

[dependencies]

serde = { version = "1", features = ["derive"] }

fluxlock-core = { path = "../fluxlock-core" }

fluxlock-storage = { path = "../fluxlock-storage" }